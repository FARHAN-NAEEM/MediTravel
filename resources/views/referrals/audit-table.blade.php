<div class="referral-table-wrap"><table class="referral-table"><thead><tr><th>সময়</th><th>ব্যবহারকারী</th><th>কাজ</th><th>কারণ / নোট</th><th>পরিবর্তন</th></tr></thead><tbody>@forelse($audits as $audit)<tr><td>{{ $audit->created_at->format('d M Y H:i') }}</td><td>{{ $audit->actor?->name ?? 'এজেন্ট / রোগী' }}</td><td>{{ $audit->action }}</td><td>{{ $audit->note }}</td><td>@if($audit->changes)<details><summary class="referral-link">বিস্তারিত</summary><pre class="referral-json">{{ json_encode($audit->changes, JSON_PRETTY_PRINT | JSON_UNESCAPED_UNICODE) }}</pre></details>@endif</td></tr>@empty<tr><td colspan="5">কোনো ইতিহাস নেই।</td></tr>@endforelse</tbody></table></div>
